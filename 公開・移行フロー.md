# 公開・移行フロー

**Local での開発が終わり、本番（またはクライアント環境）へ渡すとき**に読むファイル。

着手前のセットアップは **[案件開始フロー.md](案件開始フロー.md)**、コーディング中は **[GUIDE.md](GUIDE.md)** を参照。

---

## 目次

- [0. 全体の流れ](#0-全体の流れ)
- [1. 公開前の最終確認（共通）](#1-公開前の最終確認共通)
- [2. ビルド（`npm run build`）](#2-ビルドnpm-run-build)
- [3. WordPress 案件 — All-in-One WP Migration](#3-wordpress-案件--all-in-one-wp-migration)
- [4. LP / 静的 HTML 案件](#4-lp--静的-html-案件)
- [5. 本番・移行先での確認](#5-本番移行先での確認)
- [6. よくある質問](#6-よくある質問)

---

## 0. 全体の流れ

```
開発中（npm run dev）
    ↓
コーディング・コンテンツ・動作確認が終わった
    ↓
★ 公開前チェック（このファイルの 1〜2） ★
    ↓
移行（WP → AIOM / LP → public または FTP）
    ↓
本番で最終確認 → 納品
```

| 案件 | 移行手段 | 渡すもの |
|---|---|---|
| **WordPress** | **All-in-One WP Migration** | `.wpress`（サイト一式） |
| **LP / 静的 HTML** | FTP / ホスティングのアップロード | **`public/`** フォルダ |

> **All-in-One WP Migration**（サイト移行）と **All in One SEO（AIOSEO）**（SEO プラグイン）は別物です。

---

## 1. 公開前の最終確認（共通）

Local（または開発環境）で、エクスポート・アップロードの **前** に確認する。

### 表示・動作

- [ ] トップ・主要下層ページが意図どおり表示される
- [ ] SP / PC 両方でレイアウト崩れがない
- [ ] 画像・WebP が読み込まれている
- [ ] リンク・CTA・ドロワー・Swiper 等の JS が動く
- [ ] （WP）Contact Form 7 の送信テスト
- [ ] （WP）AIOSEO の title / description が主要ページに入っている

### Gulp / アセット

- [ ] `dev/public/assets/css/style.css` が最新
- [ ] `dev/public/assets/js/bundle.js` が最新
- [ ] `dev/public/assets/img/` に必要な画像・WebP がある
- [ ] **`npm run dev` を停止**（エクスポート・アップロード前）

### WordPress のみ

- [ ] 固定ページ・メニュー・ウィジェット等の設定が完了
- [ ] 不要な下書き・テストページを整理（任意）
- [ ] パーマリンク設定を確認（通常「投稿名」）

---

## 2. ビルド（`npm run build`）

### いつ実行する？

**All-in-One WP Migration でエクスポートする直前**（または LP の `public/` をアップロードする直前）。

| タイミング | build 必要？ |
|---|---|
| 案件開始時 | **不要**（`npm run dev` で十分） |
| 公開・移行前 | **推奨**（1 回） |

### 必須ではない理由

開発中ずっと `npm run dev` で watch していれば、`dev/public/` に CSS / JS / 画像は揃っている。**build しなくてもサイトは動く**。

### 推奨する理由

| 項目 | dev | build |
|---|---|---|
| JS（webpack） | development（やや大きい） | **production（圧縮・最適化）** |
| CSS | 通常出力 | **メディアクエリ結合（gcmq）** |
| 画像 | 変更時に処理済みなら OK | 全件の最終同期 |

> WP テーマは `style.css` と `filemtime()` で読み込んでいるため、`style.min.css` や HTML の cacheBusting は **現状使っていない**。

### 実行方法

```bash
cd dev   # テーマ内の dev/、または LP プロジェクトの dev/
npm run build
```

- [ ] エラーなく完了する
- [ ] `dev/public/assets/` が更新されている

### TinyPNG について

`npm run build` を何度実行しても、**同じソース画像なら API は再消費されない**（`dev/src/assets/img/.tinypng-sigs` で管理）。移行前に build しても、画像の中身を変えていなければ問題なし。

---

## 3. WordPress 案件 — All-in-One WP Migration

### 3-1. エクスポート（Local）

1. [ ] 上記 **1. 最終確認** と **2. build（推奨）** を完了
2. [ ] Local の WordPress 管理画面にログイン
3. [ ] **All-in-One WP Migration → エクスポート**
4. [ ] **ファイル** を選び、`.wpress` をダウンロード
5. [ ] ファイルサイズ・主要プラグインが含まれていることを確認

**エクスポートに含まれる主なもの**

| 含まれる | 備考 |
|---|---|
| データベース | 投稿・設定・メニュー等 |
| テーマ（`dev/public/` 含む） | CSS / JS / 画像はここ |
| プラグイン | AIOSEO、CF7、AIOM 等 |
| `uploads/` | メディアライブラリ |

**本番では不要だが、エクスポートに入りがちなもの**

| パス | 対応 |
|---|---|
| `dev/node_modules/` | 容量が増えるだけ。気になる場合はエクスポート前に削除可 |
| `dev/.env` | API キー入り。**本番に渡す相手によっては削除を検討** |
| `dev/src/assets/img/.tinypng-sigs` | 害はない（Git 管理外のローカル用ファイル） |

### 3-2. インポート（本番 / ステージング）

1. [ ] 移行先に WordPress を用意
2. [ ] **All-in-One WP Migration** をインストール・有効化
3. [ ] **インポート** → `.wpress` をアップロード
4. [ ] 完了後、管理画面にログインできることを確認
5. [ ] **設定 → パーマリンク** を開き、何も変えず **変更を保存**（リライトルール再生成）

> ファイルサイズ上限に引っかかる場合は、移行先の PHP `upload_max_filesize` / `post_max_size` を上げるか、AIOM の拡張機能を利用する。

---

## 4. LP / 静的 HTML 案件

WordPress を使わない案件は **`public/` をそのままサーバーへ置く**。

### 4-1. 公開前

1. [ ] **1. 最終確認** を完了
2. [ ] **（推奨）`npm run build`**
3. [ ] `npm run dev` を停止

### 4-2. アップロード

- [ ] **`public/` の中身** をサーバーの公開ディレクトリへ FTP / SFTP / ホスティングのファイルマネージャでアップロード
- [ ] `index.html` がドキュメントルートにあることを確認
- [ ] 画像パスが `./assets/img/...` 形式のままであること（PHP パスは使わない）

**アップロード不要なもの**

| パス | 理由 |
|---|---|
| `src/` | ソース（開発用） |
| `node_modules/` | ビルド環境のみ |
| `.env` | ローカル設定 |

---

## 5. 本番・移行先での確認

インポート / アップロード後、**本番 URL** で確認する。

- [ ] トップ・主要下層が表示される
- [ ] CSS / JS が読み込まれている（DevTools の Network で 404 がない）
- [ ] 画像・WebP が表示される
- [ ] SP / PC 表示
- [ ] （WP）フォーム送信
- [ ] （WP）管理画面ログイン
- [ ] （WP）AIOSEO の meta がフロントに反映されている
- [ ] ファビコン・OGP（設定している場合）

**URL が変わった場合（Local → 本番ドメイン）**

- All-in-One WP Migration は通常、URL も一緒に書き換える
- 表示がおかしい場合は **設定 → 一般** の「WordPress アドレス」「サイトアドレス」を確認

---

## 6. よくある質問

### Q. 「納品前」とはいつ？

**Local で作業が終わり、All-in-One WP Migration でエクスポート（または LP の `public/` アップロード）する直前** のこと。

### Q. 案件開始時に `npm run build` は必要？

**不要。** `npm run dev` だけで開発開始してよい。build は **公開・移行前** に 1 回推奨。

### Q. build を忘れたまま AIOM した。どうする？

多くの場合 **そのまま動く**。気になる場合は Local で `npm run build` → 再度エクスポート。

### Q. `dev/public/` は Git に入っていないが大丈夫？

**問題なし。** AIOM はテーマフォルダごとエクスポートするため、`dev/public/` も本番に載る。Git 管理外でも移行できる。

### Q. 本番サーバーで `npm run dev` は動かす？

**動かさない。** 本番は `dev/public/` の静的ファイルだけで表示する。Gulp は Local の開発環境専用。

---

## クイックリファレンス

```
開発中              → npm run dev（案件開始フロー.md）
公開・移行前        → 最終確認 → npm run build（推奨）→ dev 停止
WP 移行             → All-in-One WP Migration エクスポート → 本番インポート
LP / 静的           → public/ をサーバーへアップロード
本番確認            → 表示・フォーム・SEO・パーマリンク
着手前              → 案件開始フロー.md
コーディング中      → GUIDE.md
```
