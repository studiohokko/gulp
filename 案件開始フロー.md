# 案件開始フロー

**案件が始まったとき**に読むファイル。テンプレのコピーから開発環境の起動・初期設定まで。

着手後のコーディング（スニペット・CF7・AI の使い方等）は **[GUIDE.md](GUIDE.md)** を参照。

---

## 目次

- [0. 案件タイプを決める](#0-案件タイプを決める)
- [1. WordPress 案件](#1-wordpress-案件)
- [2. LP / 静的 HTML 案件](#2-lp--静的-html-案件)
- [3. 初期設定（共通）](#3-初期設定共通)
- [4. 完了確認](#4-完了確認)
- [5. 次に読むもの](#5-次に読むもの)

---

## 0. 案件タイプを決める

| 案件 | 使うテンプレ | BrowserSync |
|---|---|---|
| **WordPress** | `dev_liquid/` + `wordpress初期設定/` | `.env` の `BS_PROXY` = Local URL |
| **LP / 静的 HTML** | `dev_liquid/` または `dev/` | `BS_PROXY` = **空** |

**レイアウトの選び方**

| | 使うフォルダ | いつ |
|---|---|---|
| リキッド（基本） | `dev_liquid/` | WP も LP も基本はこちら |
| px 固定 | `dev/` | デザインが px 固定指定の案件 |

---

## 1. WordPress 案件

### 1-1. Local でサイトを用意

- [ ] Local で新規サイト作成（または既存サイトを使用）
- [ ] サイト URL を控える（例: `http://cosmo.local/`）
- [ ] 管理画面にログインできることを確認

### 1-2. テンプレをコピー

コピー元: **`~/Desktop/GULP/`**

| コピー元 | コピー先 |
|---|---|
| `dev_liquid/` | テーマ内 **`dev/`** |
| `wordpress初期設定/` | テーマルート |
| `.cursor/rules/` | テーマ内 `.cursor/rules/`（テーマだけ Cursor で開く場合） |

> GULP 上の名前は `dev_liquid` だが、テーマ内では必ず **`dev/`** にリネームする。

### 1-3. 環境設定

- [ ] `dev/.env.example` → `dev/.env` にコピー
- [ ] `BS_PROXY` を Local の URL に設定（末尾 `/` あり）
- [ ] `TINYPNG_API_KEY` を設定（画像圧縮を使う場合）

```env
BS_PROXY=http://example.local/
BS_PORT=8080
```

### 1-4. 開発環境を起動

```bash
cd dev
npm install
npm run dev
```

- [ ] BrowserSync が開く
- [ ] Local サイトがプロキシ表示される

### 1-5. WordPress テーマの初期設定

- [ ] Local の **外観 → テーマ** でコピーしたテーマを有効化
- [ ] `style.css` のテーマ名・説明を案件用に更新
- [ ] `404.php` は `wordpress初期設定` 同梱（存在しない URL で表示確認）
- [ ] `_p-404.scss` は `dev_liquid` に同梱済み（`project/_index.scss` に `@use 'p-404'` あり）

---

## 2. LP / 静的 HTML 案件

### 2-1. テンプレをコピー

- [ ] **`dev_liquid/`**（リキッド）または **`dev/`**（px 固定）をプロジェクトにコピー

### 2-2. 環境設定

- [ ] `.env.example` → `.env` にコピー
- [ ] **`BS_PROXY` を空** にする
- [ ] `TINYPNG_API_KEY` を設定（使う場合）

### 2-3. ソースを配置

- [ ] `src/` に `index.html` 等を置く
- [ ] 画像パスは `./assets/img/...`（PHP パスは使わない）

### 2-4. 開発環境を起動

```bash
npm install
npm run dev
```

- [ ] `public/` が BrowserSync で配信される

---

## 3. 初期設定（共通）

Figma が確定したら、以下を案件用に更新する。

### `_variables.scss`

`dev/src/assets/scss/global/_variables.scss` の **【案件ごと】** を更新。

| 項目 | 例 |
|---|---|
| カンプ幅 | `$pc-camp`, `$sp-camp`（375 / 390 等） |
| SP 余白 | `$sp-sidePadding` |
| インナー幅 | `$pc-inner`, `$pc-narrow-inner` |
| カラー | `$color-text`, `$color-main`, `$color-bg`, `$accent-color` |
| フォント | `$ff`, `$ff-jp`, `$ff-en`, `$ff-both` |
| ヘッダー高 | `$sp-header-height`, `$pc-header-height`（実装後に計測値で更新） |

**【固定】** の項目（`$breakpoint: 768` 等）は変更しない。

### WordPress のみ: `functions.php` のフォント（案件開始時のみ）

`_variables.scss` で使うフォントを **同じ family 名** で読み込む。

```php
// 例: Noto Sans JP（Google Fonts）
wp_enqueue_style('google-fonts', '//fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&display=swap', ...);
```

- フォント確定後は **以降 `_variables.scss` だけ触れば OK**

### その他（必要に応じて）

- [ ] 案件固有カラー変数（`$color-accent-001` 等）を `_variables.scss` に追加
- [ ] ページ個別上書きが必要なら `style.scss` に `@use 'customize'` を追加

---

## 4. 完了確認

- [ ] `npm run dev` がエラーなく動く
- [ ] SCSS を保存すると CSS が更新される
- [ ] BrowserSync でリロードされる
- [ ] （WP）テーマが有効化され、フロントが表示される
- [ ] （WP）アセットパス `get_theme_file_uri('dev/public/assets/...')` で画像・CSS が読める

---

## 5. 次に読むもの

**ここまで完了したら案件着手。** 以降は [GUIDE.md](GUIDE.md) を見ながらコーディングする。

| やること | 参照 |
|---|---|
| スニペット・CF7・ACF | GUIDE.md |
| AI の依頼の出し方 | GUIDE.md → ai-workflow.mdc |
| コーディング規約（AI 向け） | `.cursor/rules/coding-guide.mdc` |
| WP 管理画面を AI に操作 | `prompts/wp-admin-playwright.md` |
| Figma → コード / 固定ページ | `prompts/figma-playwright.md` |
| 画像の alt 入力 | `prompts/alt-attributes.md` |
| **公開・本番移行** | **[公開・移行フロー.md](公開・移行フロー.md)** |

---

## クイックリファレンス

```
WP 案件     → dev_liquid + wordpress初期設定 → テーマ内 dev/ → BS_PROXY あり → npm run dev
LP / 静的   → dev_liquid or dev → BS_PROXY 空 → npm run dev
着手後      → GUIDE.md
公開・移行  → 公開・移行フロー.md
```
